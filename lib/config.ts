// Numero WhatsApp per il contatto diretto con Alessandro (footer, form contatti).
export const WHATSAPP_OWNER_NUMBER = '393806599140';

// Link Calendly per prenotare una call di 15 minuti.
export const BOOKING_CALL_URL: string =
  process.env.NEXT_PUBLIC_CALENDLY_URL || 'https://calendly.com/alepunzi';

// Dominio di produzione: base per canonical, og:url e og:image.
export const SITE_URL: string =
  process.env.NEXT_PUBLIC_SITE_URL || 'https://alessandropunzi-systems.vercel.app';
